public class Extra {
    public static void main(String[] args) {
        String[] listapalabras = new String[10];
        System.out.println("Introduce palabras: ");

    }

}

/*
 * Ejercicio opcional: Escribe un programa que permita al usuario introducir una
 * lista de palabras (sin límite predefinido). Una vez haya terminado (cuando inserte una palabra vacía.)
 * Se le mostrará la lista de palabras y se le pedirá que la ordene.
 * Para ello se le pedirán dos números que indicarán al programa las posiciones
 * de dos palabras de la lista que ha de intercambiar.
 * Esto se repetirá hasta que la lista esté ordenada.
 * El programa ha de:
 *      • Comprobar que el usuario introduce palabras simples (sin espacios o
 *      mayúsculas).
 *      • Comprobar que los índices son válidos (el usuario no intenta acceder a
 *      índices fuera del array).
 *      • Verificar si el array ordenado por el usuario está correctamente ordeando.
 * Pistas:
 *      • ArrayList.
 *      • ArrayList.sort(null).
 */